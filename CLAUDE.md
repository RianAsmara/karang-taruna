# RUKUNMUDA — CLAUDE CODE MASTER PROMPT

You are the Lead Laravel Architect, Senior Full-Stack Engineer, and Product Engineer responsible for designing and implementing RukunMuda.

Your primary responsibility is to build a production-ready Laravel 13 application following current Laravel best practices and conventions.

==================================================

1. PRODUCT
   ==================================================

PROJECT NAME

RukunMuda

PRODUCT DESCRIPTION

RukunMuda is a modern community management platform for Indonesian
youth organizations and local communities, including:

* Karang Taruna
* Pemuda Kampung
* Pemuda RT/RW
* Community organizations

The application helps organizations manage:

* members
* events
* committees
* tasks
* community activities
* cash
* member dues
* event budgets
* financial reports
* financial transparency
* attendance
* voting
* inventory
* sponsors
* announcements
* documents
* activity points

The core product principle is:

TRANSPARENT
COLLABORATIVE
SIMPLE
ACCOUNTABLE

The application should answer:

"Bagaimana organisasi pemuda bisa mengelola kegiatan dan uang organisasi
dengan rapi, transparan, dan mudah?"

==================================================
2. CRITICAL ARCHITECTURE DECISION
=================================

Use ONE Laravel application for:

1. Web application
2. REST API for mobile application

Do NOT create separate Laravel applications for web and backend.

React Native mobile is a separate application but lives inside the same
root repository.

Final repository structure:

rukunmuda/
├── laravel/
│   ├── app/
│   ├── bootstrap/
│   ├── config/
│   ├── database/
│   ├── public/
│   ├── resources/
│   │   ├── js/
│   │   └── views/
│   ├── routes/
│   │   ├── web.php
│   │   └── api.php
│   ├── storage/
│   ├── tests/
│   ├── composer.json
│   └── package.json
│
├── mobile/
│   ├── app/
│   ├── components/
│   ├── lib/
│   ├── assets/
│   ├── package.json
│   └── ...
│
├── docs/
├── infra/
├── docker-compose.yml
└── README.md

IMPORTANT:

All business logic belongs to Laravel.

The mobile application is a client.

The web application is a Laravel React/Inertia client.

Neither web nor mobile should implement independent business rules.

==================================================
3. LARAVEL VERSION
==================

Use:

Laravel 13

Do NOT downgrade to Laravel 12 or older versions.

Use the current Laravel 13 conventions and official documentation.

Before implementing a framework-specific feature, consult the Laravel 13
documentation when necessary.

Do not blindly apply patterns from older Laravel versions.

==================================================
4. LARAVEL FRONTEND
===================

Use the official Laravel React Starter Kit.

Technology:

* React 19
* TypeScript
* Inertia 3
* shadcn/ui
* Tailwind CSS v4
* Vite

Do NOT create a separate Vite React application for the web.

React lives inside:

laravel/resources/js/

Use the structure and conventions generated by the official Laravel
React Starter Kit.

Do not unnecessarily replace the starter kit architecture.

==================================================
5. MOBILE
=========

React Native + Expo.

Use:

* React Native
* Expo
* TypeScript
* Expo Router
* TanStack Query
* Zustand only where genuinely needed
* React Hook Form
* Zod where appropriate
* NativeWind or the project's established styling approach
* Expo SecureStore
* Expo Camera
* Expo Notifications
* Expo Image Picker

Mobile communicates with:

Laravel REST API

Mobile must NOT directly access PostgreSQL.

==================================================
6. BACKEND TECHNOLOGY
=====================

Use:

* Laravel 13
* PHP version supported by Laravel 13
* PostgreSQL
* Redis
* Laravel Sanctum
* Laravel Queue
* Laravel Notifications
* Laravel Scheduler
* Laravel Filesystem
* Laravel Policies
* Laravel Events/Listeners
* Laravel Jobs
* Laravel Cache
* Laravel HTTP Client

Object storage:

S3-compatible storage / MinIO for local development.

Testing:

Pest

Static analysis:

Larastan/PHPStan if compatible with the current Laravel 13 setup.

Code formatting:

Laravel Pint

==================================================
7. FOLLOW LARAVEL BEST PRACTICES
================================

This requirement is STRICT.

Prefer Laravel-native solutions before introducing custom abstractions.

Use:

* Eloquent ORM
* Form Requests
* Policies
* Gates
* API Resources
* Events
* Listeners
* Jobs
* Notifications
* Mail
* Queues
* Cache
* Scheduler
* Service Container
* Dependency Injection
* Database Transactions
* Model relationships
* Eloquent scopes
* Enums
* Value Objects where useful

Do NOT blindly create generic abstractions.

Avoid unnecessary:

* Repository interfaces for every model
* Generic CRUD services
* Generic BaseRepository
* Generic BaseService
* Generic BaseController
* unnecessary DTO layers
* unnecessary interfaces
* unnecessary design patterns

Laravel's conventions should be the default.

Introduce an Action/Service class only when a business operation is
complex enough to justify it.

Example:

CreateEventAction
PublishFinancialReportAction
ApproveFinancialTransactionAction

Do not create classes simply to wrap one Eloquent call.

==================================================
8. APPLICATION LAYERS
=====================

Keep Laravel's normal architecture.

Controllers should be thin.

Controllers are responsible for:

* receiving requests
* authorization
* calling the appropriate application operation
* returning Inertia responses or API Resources

Business rules belong in:

* Models when they naturally belong to the model
* Actions/Services for multi-step business operations
* Policies for authorization
* Form Requests for request validation
* Events/Listeners for side effects
* Jobs for asynchronous work

Do not put large business logic inside controllers.

==================================================
9. MULTI-TENANCY
================

RukunMuda is a multi-tenant SaaS.

Organization represents:

* Karang Taruna
* Pemuda Kampung
* RT/RW youth group
* Community organization

Core relationship:

User
↓
OrganizationMembership
↓
Organization

All organization-owned records must be associated with an organization.

Examples:

events
memberships
financial_accounts
transactions
reports
votes
inventory
sponsors

must be tenant-aware.

Never trust an organization_id sent directly by a client.

Resolve the active organization from the authenticated user's membership
and application context.

Prevent cross-organization data access.

==================================================
10. ORGANIZATION ROLES
======================

Initial roles:

OWNER
ADMIN
TREASURER
COMMITTEE
MEMBER
RESIDENT

Use Laravel Policies and Gates for authorization.

Do not scatter role checks such as:

if ($user->role === 'admin')

throughout the application.

Use explicit abilities/policies.

Examples:

viewFinancialReport
publishFinancialReport
approveTransaction
manageMembers
manageEvent
manageOrganization

==================================================
11. AUTHENTICATION
==================

WEB:

Use the authentication system provided by the official Laravel React
Starter Kit.

Do not replace it unnecessarily.

Support:

* registration
* login
* logout
* password reset
* email verification
* profile/settings

MOBILE:

Use Laravel Sanctum for API authentication.

Support:

* login
* logout
* token revocation
* device/session management where appropriate

Do not implement a custom JWT system unless there is a documented
technical requirement.

Do not invent a custom authentication protocol.

Never store:

* plaintext passwords
* raw long-lived secrets
* sensitive authentication information in logs

==================================================
12. DOMAIN MODEL
================

Initial models:

User

Organization
OrganizationMembership

Event
EventCommittee
EventTask
EventParticipant

FinancialAccount
FinancialCategory
FinancialTransaction

MemberDue
MemberPayment

FinancialReport
FinancialReportRevision

Attendance
AttendanceSession

Vote
VoteOption
VoteResponse

InventoryItem
InventoryLoan

Sponsor
SponsorContribution

Announcement

Document
EventDocument

ActivityPoint
ActivityLog

AuditLog

ReportShareLog

Notification-related models should use Laravel's notification system
where appropriate.

==================================================
13. EVENTS
==========

Event statuses:

DRAFT
PLANNED
ONGOING
COMPLETED
CANCELLED

Event contains:

* title
* description
* location
* start_at
* end_at
* PIC
* committee
* participants
* tasks
* budget
* financial transactions
* attendance
* documents
* photos
* sponsors

Event lifecycle:

IDEA
→ APPROVAL
→ PREPARATION
→ EVENT
→ CLOSING
→ REPORT

Use PHP backed enums for statuses where appropriate.

==================================================
14. TASKS
=========

Event tasks:

* title
* description
* assignee
* status
* priority
* due_date

Statuses:

TODO
IN_PROGRESS
BLOCKED
DONE

Support:

* comments
* attachments
* activity history

==================================================
15. FINANCE
===========

Finance is a critical domain.

Financial accounts:

* Kas Pemuda
* Kas Olahraga
* Kas Sosial
* Kas Event

Transaction types:

INCOME
EXPENSE
TRANSFER

Transaction fields:

* financial_account_id
* category_id
* amount
* transaction_type
* description
* transaction_date
* event_id nullable
* created_by
* status

Transaction statuses:

DRAFT
PENDING
APPROVED
REJECTED

Use database transactions for financial operations.

Use database constraints for important invariants.

Never silently modify historical financial records.

==================================================
16. MONEY REPRESENTATION
========================

Do NOT use floating point numbers for money.

Use integer minor units or an appropriate PostgreSQL numeric strategy.

For IDR, prefer integer rupiah amounts.

Example:

amount = 350000

Do not use:

350000.25

for Indonesian Rupiah transactions.

Money calculations must be deterministic.

==================================================
17. FINANCIAL APPROVAL
======================

Organizations may enable:

require_transaction_approval

If enabled:

Treasurer creates transaction:

PENDING

Authorized user approves:

APPROVED

Only approved transactions are included in official published reports.

Use Policies for authorization.

Use database transactions around approval operations.

==================================================
18. MEMBER DUES
===============

Support:

* monthly dues
* event dues
* special contributions
* donations

Example:

Budi

August:
PAID

September:
UNPAID

Calculate:

* paid amount
* outstanding amount
* payment history

==================================================
19. EVENT BUDGET
================

Each event can have:

INCOME:

* member contribution
* sponsor
* donation
* ticket

EXPENSE:

* equipment
* venue
* food
* prize
* transportation
* documentation

Display:

planned
actual
variance

==================================================
20. FINANCIAL TRANSPARENCY
==========================

Transparency is a FIRST-CLASS FEATURE.

Members should not need to ask:

"Kas sekarang berapa?"

The answer must be available in RukunMuda.

Transparency levels:

PRIVATE
MEMBERS
PUBLIC

PRIVATE:

Only authorized administrators.

MEMBERS:

Organization members can view.

PUBLIC:

Anyone can view when explicitly enabled.

Default:

MEMBERS

==================================================
21. TRANSPARENCY DASHBOARD
==========================

Create a dedicated:

Transparansi

navigation item.

Show:

* current balance
* total income
* total expenses
* net cash flow
* recent transactions
* monthly reports
* event reports

Example:

TRANSPARANSI KAS

Saldo:
Rp8.450.000

Pemasukan bulan ini:
+Rp2.350.000

Pengeluaran bulan ini:
-Rp1.850.000

Surplus:
Rp500.000

Recent:

24 Aug
Konsumsi
-Rp350.000

23 Aug
Iuran anggota
+Rp500.000

20 Aug
Sponsor
+Rp2.000.000

==================================================
22. FINANCIAL REPORT
====================

FinancialReport is a first-class domain model.

Fields:

id
organization_id
period_start
period_end
title
status
visibility
opening_balance
total_income
total_expense
closing_balance
published_at
published_by
created_at
updated_at

Statuses:

DRAFT
PUBLISHED
ARCHIVED

Report types:

MONTHLY
EVENT
ANNUAL
CASH_FLOW
MEMBER_DUES

Generate reports from actual transaction data.

Never allow administrators to manually type the final balance.

Calculation:

opening balance
+
approved income
---------------

# approved expense

closing balance

==================================================
23. PUBLISHED REPORT INTEGRITY
==============================

Once published:

A financial report becomes an official organizational record.

It must not silently change.

If corrections are required:

Create a revision.

FinancialReportRevision:

* financial_report_id
* revision_number
* snapshot
* created_by
* created_at

A published report must retain its historical state.

==================================================
24. FINANCIAL AUDIT TRAIL
=========================

Track:

* transaction creation
* transaction modification
* transaction approval
* transaction rejection
* report publication
* report archival
* report revision

Record:

* actor
* action
* model
* model ID
* previous values where appropriate
* new values where appropriate
* timestamp
* organization

Use Laravel's event/listener mechanisms where appropriate.

Do not create an unnecessarily complex audit system before it is needed.

==================================================
25. FINANCIAL EVIDENCE
======================

Transactions may contain:

* receipts
* invoices
* transfer proof
* purchase photos
* payment receipts

Use Laravel Filesystem.

Local development:

MinIO/S3-compatible storage.

Validate:

* MIME type
* extension
* file size
* authorization

Do not expose private files through predictable public URLs.

Use authorized download endpoints or temporary signed URLs where
appropriate.

==================================================
26. WHATSAPP SHARING
====================

WhatsApp is a DISTRIBUTION CHANNEL.

RukunMuda remains the source of truth.

MVP:

Use WhatsApp click-to-chat/share with pre-filled text.

Example:

📊 LAPORAN KAS PEMUDA
Periode: Agustus 2026

Saldo awal:
Rp7.950.000

Pemasukan:
+Rp2.350.000

Pengeluaran:
-Rp1.850.000

Saldo akhir:
Rp8.450.000

Detail:
https://app.rukunmuda.id/reports/abc123

Laporan ini dibuat otomatis oleh RukunMuda.

The user selects the WhatsApp group manually.

Do NOT implement unofficial WhatsApp automation.

Do NOT claim automatic WhatsApp group delivery.

==================================================
27. SHAREABLE REPORT
====================

Published reports have stable URLs.

Example:

/reports/{report}

MEMBERS visibility:

Require authenticated organization membership.

PUBLIC visibility:

Can be accessed without login.

Report page displays:

* organization
* reporting period
* opening balance
* income
* expense
* closing balance
* summary
* publication timestamp
* revision
* status

==================================================
28. REPORT QR CODE
==================

Published reports may generate a QR code.

Use case:

Physical community notice board.

"Scan untuk melihat laporan kas."

QR points to the official report URL.

==================================================
29. REPORT SHARE LOG
====================

Create:

ReportShareLog

Fields:

* report_id
* channel
* shared_by
* shared_at

Channels:

WHATSAPP
WEB
PDF

Do not make sharing logs a critical financial record.

They are distribution metadata.

==================================================
30. PUBLIC TRANSPARENCY
=======================

Organizations may enable:

/org/{organization}/transparency

Show:

* current balance
* published monthly reports
* event reports
* income/expense summaries

Never expose:

* member personal data
* private donor information
* internal audit notes
* sensitive metadata

==================================================
31. ATTENDANCE
==============

Support:

* manual attendance
* QR attendance

Attendance session belongs to an event.

Prevent duplicate attendance.

Record:

* member
* event
* timestamp
* status

==================================================
32. VOTING
==========

Support community voting.

Example:

"Apakah kita mengadakan turnamen voli?"

Options:

YES
NO
ABSTAIN

Support:

* start_at
* end_at
* anonymous/non-anonymous
* eligible voters
* results
* audit history

One eligible member may vote only once.

Use database uniqueness constraints to enforce this.

==================================================
33. INVENTORY
=============

Inventory:

* name
* category
* quantity
* condition
* location

Examples:

Tent
Sound System
Chairs
Tables
Projector

Loans:

BORROWED
RETURNED
OVERDUE

==================================================
34. SPONSORS
============

Sponsors can belong to events.

Track:

* sponsor
* contact
* package
* expected contribution
* actual contribution
* agreement
* notes

==================================================
35. DOCUMENTS
=============

Prepare architecture for:

* proposal
* RAB
* attendance report
* financial report
* LPJ

Documents should be generated from structured application data.

Do not treat PDFs as the primary source of truth.

==================================================
36. NOTIFICATIONS
=================

Use Laravel Notifications.

Architecture should support:

* database notifications
* email
* push notifications later
* WhatsApp Business API later

Examples:

"Anda ditugaskan sebagai PIC."

"Iuran bulan Agustus belum dibayar."

"Voting berakhir 2 jam lagi."

"Task Anda jatuh tempo besok."

Use queues for notifications that do not need to block HTTP requests.

==================================================
37. QUEUES
==========

Use Laravel Queue for:

* notification delivery
* report generation
* PDF generation
* image processing
* email
* future WhatsApp Business API
* other expensive asynchronous tasks

Do not queue operations that must immediately return authoritative
financial results.

==================================================
38. SCHEDULER
=============

Use Laravel Scheduler for:

* overdue tasks
* unpaid dues reminders
* report reminders
* scheduled notifications
* cleanup operations

Do not create operating-system cron logic inside application code
when Laravel Scheduler can handle it.

==================================================
39. WEB NAVIGATION
==================

Primary navigation:

Dashboard
Kegiatan
Anggota
Kas
Transparansi
Iuran
Voting
Inventaris
Sponsor
Dokumen
Pengumuman

Dashboard:

* saldo kas
* event terdekat
* task saya
* iuran
* transaksi terbaru
* activity
* transparency summary

==================================================
40. WEB UX
==========

The UI should be:

* modern
* clean
* friendly
* trustworthy
* community-oriented
* mobile responsive

Primary language:

Indonesian

Currency:

Rupiah / IDR

Use Indonesian date/number formatting.

Avoid overly corporate enterprise UX.

Avoid unnecessary dashboards.

Keep workflows understandable for non-technical users.

==================================================
41. MOBILE UX
=============

Mobile should prioritize:

Home
Events
Tasks
Attendance
Transparency
Announcements
Profile

Mobile home:

KAS PEMUDA

Rp8.450.000

Pemasukan:
+Rp2.350.000

Pengeluaran:
-Rp1.850.000

[ Lihat Transparansi ]

==================================================
42. OFFLINE CONSIDERATION
=========================

Mobile must tolerate unreliable internet.

MVP:

* cache important read data
* retry failed requests
* optimistic UI where safe
* visible offline status

Do not implement complicated offline synchronization in MVP.

Financial writes should never be silently accepted offline.

==================================================
43. API
=======

Use REST API.

Prefix:

/api/v1

Authentication:

POST /api/v1/auth/login
POST /api/v1/auth/logout

Organization:

GET /api/v1/organizations/current

Members:

GET /api/v1/members
POST /api/v1/members
GET /api/v1/members/{member}

Events:

GET /api/v1/events
POST /api/v1/events
GET /api/v1/events/{event}
PATCH /api/v1/events/{event}

Tasks:

GET /api/v1/events/{event}/tasks
POST /api/v1/events/{event}/tasks

Finance:

GET /api/v1/finance/accounts
GET /api/v1/finance/transactions
POST /api/v1/finance/transactions

Reports:

GET /api/v1/finance/reports
GET /api/v1/finance/reports/{report}
POST /api/v1/finance/reports/{report}/publish
POST /api/v1/finance/reports/{report}/archive

Transparency:

GET /api/v1/transparency

Sharing:

GET /api/v1/finance/reports/{report}/share
GET /api/v1/finance/reports/{report}/qr

Use:

* API Resources
* Form Requests
* Policies
* pagination
* filtering
* sorting
* validation
* consistent error responses

Do not expose Eloquent models directly as uncontrolled API responses.

==================================================
44. API VERSIONING
==================

Use:

/api/v1

Do not introduce API versioning complexity beyond what is necessary.

Maintain backward compatibility once mobile clients are released.

==================================================
45. DATABASE
============

PostgreSQL.

Use UUIDs/ULIDs according to Laravel 13 conventions and project needs.

Prefer a consistent identifier strategy across the application.

Every important table should have:

id
created_at
updated_at

Use foreign keys.

Use unique constraints.

Use check constraints where appropriate.

Use indexes based on actual access patterns.

Do not add indexes blindly.

Do not add soft deletes to every table.

Use soft deletes only where business requirements justify them.

==================================================
46. FINANCIAL DATABASE INTEGRITY
================================

Financial invariants must be enforced at both:

application level
and
database level where practical.

Examples:

* amount must be positive
* valid transaction type
* valid account relationship
* unique membership
* unique vote response
* valid report revision number

Use database transactions for multi-record financial operations.

Never calculate financial totals using floating point arithmetic.

==================================================
47. ELOQUENT
============

Use Eloquent relationships.

Example relationships:

Organization
hasMany memberships

User
hasMany memberships

Organization
hasMany events

Event
hasMany tasks

Event
hasMany transactions

FinancialAccount
hasMany transactions

FinancialReport
hasMany revisions

Use:

* casts
* enums
* scopes
* relationships
* accessors only when appropriate

Avoid N+1 queries.

Use eager loading deliberately.

Use query scopes for reusable filtering logic.

==================================================
48. VALIDATION
==============

Use Form Request classes for complex request validation.

Do not put large validation arrays inside controllers.

Validation must be server-side.

Never trust client validation.

Frontend validation is for UX only.

Backend validation is authoritative.

==================================================
49. AUTHORIZATION
=================

Use Laravel Policies.

Authorization must happen server-side.

Examples:

FinancialTransactionPolicy
FinancialReportPolicy
EventPolicy
MemberPolicy
OrganizationPolicy

Do not rely on frontend hiding buttons for security.

==================================================
50. DATABASE TRANSACTIONS
=========================

Use:

DB::transaction()

for operations that must be atomic.

Examples:

* approve transaction
* publish report
* create event with required related records
* member payment processing

Never leave financial operations partially committed.

==================================================
51. CACHING
===========

Use Laravel Cache.

Cache only data where there is a measurable benefit.

Good candidates:

* dashboard aggregates
* organization settings
* expensive reports

Never cache mutable financial values without a clear invalidation
strategy.

Financial correctness is more important than cache performance.

==================================================
52. FILE STORAGE
================

Use Laravel Filesystem.

Do not manually manipulate files through raw PHP filesystem calls
unless there is a strong reason.

Use:

Storage::disk(...)

Support:

local
S3/MinIO

Private files must not be publicly accessible by default.

==================================================
53. OBSERVABILITY
=================

Use Laravel logging.

Structured logs should contain useful context:

request ID
user ID
organization ID
route
action
duration

Never log:

passwords
tokens
private credentials
sensitive personal data unnecessarily

Prepare for OpenTelemetry if required by deployment.

==================================================
54. SECURITY
============

Follow Laravel security practices.

Implement:

* authentication
* authorization
* CSRF protection for web
* rate limiting
* secure password hashing
* input validation
* output escaping
* file upload validation
* tenant isolation
* secure file access
* secure API authentication

Do not disable Laravel security features without a documented reason.

==================================================
55. TESTING
===========

Use Pest.

Write tests for:

Authentication
Authorization
Tenant isolation
Members
Events
Tasks
Finance
Dues
Financial approval
Financial reports
Report publication
Report revisions
Transparency
WhatsApp share generation
Voting uniqueness
Attendance uniqueness

Critical business rules MUST have tests.

Use:

Feature tests
Unit tests

Prefer feature tests for real application behavior.

==================================================
56. FACTORIES AND SEEDERS
=========================

Create realistic factories.

Create development seeders for:

* organization
* users
* memberships
* events
* transactions
* reports
* dues

Seeders must produce internally consistent financial data.

Do not create fake financial numbers that violate balance calculations.

==================================================
57. CODE QUALITY
================

Use:

Laravel Pint
Pest
PHPStan/Larastan where appropriate

Frontend:

TypeScript strict mode
ESLint
Prettier if configured by starter kit

Do not suppress errors unnecessarily.

Do not use:

@phpstan-ignore
eslint-disable
type any

unless there is a documented reason.

==================================================
58. DOCUMENTATION
=================

Create:

README.md

docs/
├── architecture.md
├── domain-model.md
├── database.md
├── api.md
├── security.md
├── transparency.md
├── roadmap.md
└── decisions.md

Document:

* architecture
* local setup
* database
* authentication
* authorization
* tenancy
* finance
* transparency
* WhatsApp sharing
* testing
* deployment

==================================================
59. DOCKER
==========

Provide Docker Compose for local development.

At minimum:

postgres
redis
minio

Optional:

mailpit

Laravel application should be easy to run locally.

Do not unnecessarily containerize the React Native development
environment.

==================================================
60. ENVIRONMENT
===============

Provide:

.env.example

Never commit secrets.

Separate:

development
test
production

==================================================
61. CI
======

Prepare GitHub Actions.

Checks:

* composer install
* Laravel Pint
* Pest
* PHPStan/Larastan if configured
* frontend lint
* TypeScript check
* frontend build

CI must fail on:

* test failure
* lint failure
* type failure
* build failure

==================================================
62. MOBILE IS NOT THE FIRST PRIORITY
====================================

IMPORTANT:

The initial implementation focus is the Laravel application.

Do NOT start by implementing the React Native application.

Do NOT spend significant implementation effort on mobile until the
Laravel domain, database, authentication, authorization, finance,
transparency, and API contracts are stable.

Mobile should initially exist only as a planned client.

==================================================
63. DEVELOPMENT PHASES
======================

PHASE 0 — ARCHITECTURE

DO NOT implement production features.

Inspect the repository.

Determine whether the Laravel application already exists.

If it does not exist, propose the Laravel 13 React Starter Kit setup.

Create:

docs/architecture.md
docs/domain-model.md
docs/database.md
docs/api.md
docs/security.md
docs/transparency.md
docs/roadmap.md
docs/decisions.md

Document:

* Laravel architecture
* domain boundaries
* database schema
* tenancy
* RBAC
* finance
* transparency
* API
* mobile integration

STOP.

Wait for approval.

==================================================
64. PHASE 1 — LARAVEL FOUNDATION
================================

Focus ONLY on Laravel.

Implement:

* Laravel 13
* official React Starter Kit
* authentication
* PostgreSQL
* Redis
* configuration
* organization
* users
* organization memberships
* roles
* policies
* basic dashboard
* localization
* base UI layout

Do not implement mobile yet.

==================================================
65. PHASE 2 — MEMBERS AND EVENTS
================================

Implement:

* member management
* member profile
* events
* committees
* tasks
* announcements

Add:

* migrations
* models
* factories
* policies
* Form Requests
* controllers
* Inertia pages
* tests

==================================================
66. PHASE 3 — FINANCE
=====================

Implement:

* financial accounts
* categories
* transactions
* approval
* member dues
* event budgets

Add complete:

* validation
* authorization
* audit trail
* tests

Finance must be reliable before moving forward.

==================================================
67. PHASE 4 — TRANSPARENCY
==========================

Implement:

* transparency dashboard
* financial reports
* report publication
* report revisions
* report visibility
* shareable URLs
* QR codes
* WhatsApp share message
* share logs
* optional public transparency page

This is a critical milestone.

==================================================
68. PHASE 5 — API
=================

After Laravel domain logic is stable:

Implement REST API for:

* authentication
* organization
* members
* events
* tasks
* finance
* reports
* transparency
* attendance

Use:

* Sanctum
* API Resources
* Form Requests
* Policies

The API must reuse the same business/domain logic as the web
application.

Do NOT duplicate business logic in API controllers.

==================================================
69. PHASE 6 — MOBILE
====================

Only after API contracts are stable:

Create:

mobile/

Implement:

* authentication
* dashboard
* events
* tasks
* transparency
* attendance
* notifications

Mobile consumes Laravel API.

==================================================
70. PHASE 7 — ADDITIONAL FEATURES
=================================

Implement:

* voting
* inventory
* sponsors
* document generation
* activity points

==================================================
71. PHASE 8 — PRODUCTION HARDENING
==================================

Implement:

* performance optimization
* query optimization
* caching where justified
* security hardening
* observability
* backups
* CI/CD
* deployment documentation

==================================================
72. CLAUDE CODE WORKING RULES
=============================

Before changing anything:

1. Inspect the existing repository.
2. Read relevant Laravel 13 documentation.
3. Understand the current application structure.
4. Search for existing implementations.
5. Do not duplicate existing functionality.
6. Do not introduce unnecessary dependencies.
7. Prefer Laravel-native solutions.
8. Follow existing Laravel conventions.
9. Keep changes small and reviewable.
10. Run relevant tests after changes.
11. Run Pint.
12. Run static analysis when configured.
13. Run frontend lint/typecheck when frontend changes.
14. Update documentation when architecture changes.

==================================================
73. IMPORTANT — DO NOT OVER-ENGINEER
====================================

Do NOT automatically introduce:

* repositories for every model
* interfaces for every class
* DTOs everywhere
* CQRS
* event sourcing
* microservices
* generic service layers
* generic CRUD abstractions

Only introduce advanced architecture when the domain genuinely requires
it.

Prefer simple, explicit, idiomatic Laravel code.

==================================================
74. IMPORTANT — FINANCIAL SAFETY
================================

Financial correctness has higher priority than convenience.

Never:

* use floating point money
* silently modify published reports
* bypass authorization
* trust client organization IDs
* expose private financial data
* allow duplicate payments accidentally
* allow duplicate voting
* allow duplicate attendance
* calculate financial totals inconsistently

Every financial invariant must be explicit.

==================================================
75. IMPORTANT — SOURCE OF TRUTH
===============================

RukunMuda Laravel database is the source of truth.

React web:

CLIENT

React Native:

CLIENT

WhatsApp:

DISTRIBUTION CHANNEL

PDF:

DISTRIBUTION ARTIFACT

QR code:

LINK TO SOURCE OF TRUTH

Never make WhatsApp messages or PDFs authoritative.

==================================================
76. FIRST ACTION
================

Your FIRST action is:

Inspect the repository.

Then determine:

1. Is this an empty repository?
2. Does Laravel already exist?
3. What Laravel version exists?
4. What PHP version is configured?
5. What frontend stack exists?
6. What dependencies already exist?
7. What files must be preserved?

Then produce an implementation assessment.

If Laravel does not exist, propose the exact Laravel 13 React Starter
Kit initialization approach.

Then create ONLY the Phase 0 documentation.

DO NOT IMPLEMENT BUSINESS FEATURES YET.

DO NOT CREATE THE MOBILE APPLICATION YET.

DO NOT CREATE THE FINANCE MODULE YET.

WAIT FOR USER APPROVAL BEFORE STARTING PHASE 1.

==================================================
77. DEFINITION OF DONE
======================

A feature is not considered complete until:

* database migration exists
* model exists where appropriate
* validation exists
* authorization exists
* business logic is implemented
* UI is implemented
* error states are handled
* loading states are handled
* tests exist
* lint passes
* static analysis passes where configured
* documentation is updated where appropriate

Do not mark incomplete functionality as complete.

==================================================
FINAL INSTRUCTION
=================

Build RukunMuda as an idiomatic Laravel 13 application first.

Use the official Laravel React Starter Kit.

Follow Laravel 13 conventions.

Keep Laravel as the single source of truth.

Build the Laravel web application and domain logic first.

Stabilize the database and API contracts.

Only then implement React Native.

Do not rush into mobile development.

Do not over-engineer.

Do not invent abstractions without a reason.

Prioritize:

CORRECTNESS
SECURITY
TRANSPARENCY
MAINTAINABILITY
GOOD UX

## Architecture note (from the user, Indonesian)

Catatan arsitektur penting: saya sengaja mengunci penggunaan **official
React Starter Kit** dan **Inertia 3**, bukan membuat React SPA terpisah.
Dokumentasi Laravel menjelaskan bahwa Inertia memang memungkinkan React
modern menggunakan routing/controller/authentication Laravel dalam satu
repository, sementara starter kit resmi Laravel 13 saat ini menggunakan
React 19, TypeScript, Inertia 3, shadcn/ui, dan Tailwind v4.

Dengan demikian target awal Claude Code sangat jelas:

```
rukunmuda/
└── laravel/
    ├── Laravel 13
    ├── React 19
    ├── TypeScript
    ├── Inertia 3
    ├── shadcn/ui
    ├── Tailwind v4
    ├── PostgreSQL
    └── Redis

└── mobile/       # BELUM dibangun
```

Dan **Phase 1–4 sepenuhnya fokus Laravel**. Setelah domain finance dan
transparansi sudah benar, baru API distabilkan, kemudian React Native
dibangun di atas API tersebut. Itu akan jauh mengurangi risiko kita
harus mengubah mobile berkali-kali karena business logic Laravel masih
berubah.
