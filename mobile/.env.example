# Laravel API base URL. Default (unset) resolves to localhost:8000
# (iOS simulator/web) or 10.0.2.2:8000 (Android emulator) — see
# src/lib/api.ts. Set this explicitly for a physical device, to your
# dev machine's LAN IP:
# EXPO_PUBLIC_API_URL=http://192.168.1.23:8000/api/v1
